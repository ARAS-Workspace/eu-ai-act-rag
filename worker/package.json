{
	"name": "eu-ai-act-rag-worker",
	"version": "1.0.0",
	"description": "EU AI Act RAG Worker — Cloudflare AI Search (AutoRAG) API",
	"author": "Riza Emre ARAS",
	"license": "MIT",
	"scripts": {
		"dev": "dotenv -- wrangler dev --port 8791",
		"start": "dotenv -- wrangler dev --port 8791",
		"deploy": "dotenv -- wrangler deploy",
		"cf-typegen": "dotenv -- wrangler types"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "4.20260304.0",
		"dotenv-cli": "11.0.0",
		"typescript": "5.9.3",
		"wrangler": "4.68.1"
	},
	"dependencies": {
		"ajv": "8.18.0"
	}
}
